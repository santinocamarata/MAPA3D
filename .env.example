# Copiá este archivo a `.env` y completá la key.
# Nunca se expone al navegador: la usa sólo el proxy de Node (server/aiProxy.js).
ANTHROPIC_API_KEY=sk-ant-...

# Opcional: cambiar el modelo. Por defecto claude-sonnet-4-6.
# ANTHROPIC_MODEL=claude-sonnet-4-6
